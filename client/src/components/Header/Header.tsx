import React from 'react';
import { Nav } from '../../components';
import './style.css';

export const Header = () => {
  return (
    <header className="header">
      Header
      <Nav />
    </header>
  );
};
